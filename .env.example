# Zendesk Configuration
# Get these from your Zendesk Admin Center

# Your Zendesk subdomain (e.g., if your URL is https://acme.zendesk.com, use "acme")
ZENDESK_SUBDOMAIN=your-subdomain

# Email of an admin/agent user
ZENDESK_EMAIL=support@yourcompany.com

# API Token (create in Admin Center > Apps and integrations > APIs > Zendesk API)
ZENDESK_API_TOKEN=your-api-token

# Server port (optional, defaults to 4000)
PORT=4000
